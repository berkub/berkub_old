<div class="row">
  <h1 class="header">BERKUB</h1>
  <% unless user_signed_in? %>
    <p><%= link_to "Login", new_user_session_path %> or <%= link_to "Register", new_user_registration_path %></p>
  <% else %>
    <p><%= current_user.email %> | <%= link_to "Logout", destroy_user_session_path, data: { method: "delete" } %></p>
  <% end %>
</div>
